oimage:
  file: .gitpod.Dockerfile

tasks:
  - init: quarto check    &&
      quarto --version    &&
      R --version         &&
      tex --version       &&
      inkscape --version  &&
      gs --version 
    command: echo " you run your own apachectl -D FOREGROUND"
    
ports:
  - port: 8080
    visibility: public
    onOpen: open-browser

