ports:
- port: 8080  
  visibility: public
  onOpen: open-preview

tasks:
- init:       quarto render                                                                                                &&
              mv _book public                                                                                              &&
              apachectl start  
